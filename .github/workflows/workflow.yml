name: Workflows from other submodule

on:
  workflow_dispatch:

jobs:
  build:
    uses: fluffy-memory-blue/workflows/.github/workflows/c-cpp.yml@master
  cppcheck:
    uses: fluffy-memory-blue/workflows/.github/workflows/cppcheck.yml@master